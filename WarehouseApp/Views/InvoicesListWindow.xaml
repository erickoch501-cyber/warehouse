<Window x:Class="WarehouseApp.Views.InvoicesListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Накладные"
        Height="500" Width="800"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="WarehouseTitle" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

        <!-- Таблица накладных -->
        <DataGrid x:Name="InvoicesGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True"
                  MouseDoubleClick="InvoicesGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Номер" Binding="{Binding Number}" Width="150"/>
                <DataGridTextColumn Header="Дата" Binding="{Binding Date, StringFormat=dd.MM.yyyy HH:mm}" Width="150"/>
                <DataGridTextColumn Header="Тип" Binding="{Binding Type}" Width="100"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding IsCompleted}" Width="100"/>
                <DataGridTextColumn Header="Товаров" Binding="{Binding Items.Count}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Кнопки -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Добавить накладную" Width="130" Margin="0,0,10,0" Click="AddInvoice_Click"/>
            <Button Content="Просмотр" Width="80" Margin="0,0,10,0" Click="ViewInvoice_Click"/>
            <Button Content="Закрыть" Width="80" Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>